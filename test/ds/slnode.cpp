/*
 * PRIMERO ESCRIBE UNA PEQUEÑA DESCRIPCION DE LOS QUE CONTIENE EL ARCHIVO (BREVE DESCRIPCION, STATUS, MAINTAINER)
 * 
 */

#include "adas/ds.hpp" //esto importa la clase SLNode(que esta en la carpeta include/adas/ds)
using namespace adas::ds;  // para que simplemente uses el nombre de la clase y ya no pongas en namespace


int main()
{
	// COLOCA AQUI TU CODIGO QUE PRUEBE LOS DISTINTOS CONTRUCTORES Y METODOS DE LA CLASE SLNode
}
